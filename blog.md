# 基于 DeepSeek Harness 的 AI 版微信 - DeepChat

> GitHub：https://github.com/theajack/deepchat

你有没有过这种念头：微信的界面你很熟，但里面聊天的那群人，能不能……全换成 AI？

不是那种一个对话框、一个助手、一问一答的那种。而是**打开就是一个通讯录**，里面躺着十几个各有性格的"好友"，你可以挨个私聊，也可以拉一个群，让它们自己聊起来，你偶尔插一句，然后看它们互相接话、吵起来、甚至自己起话题。

接下来我要介绍的就是这种东西，它叫 **DeepChat**。

---

## 先说结论：它长什么样

打开 DeepChat， 它的主界面如下：

- 左边是**会话列表/好友列表**，跟微信一样，有最后一条消息预览、未读数、可以置顶
- 右边是**聊天窗口**，流式打字机输出，工具调用会展开成一张卡片，实时显示参数和结果，而不是让你干等一个转圈

但真正让它"活"起来的，是下面这几件事。

---

## 一、通讯录里的每一个好友，都是一个独立的 Agent

不是同一套 prompt 换了个名字。每个好友有：

- **人设**：一段可以自由编写的人格描述，注入到它的系统提示词里
- **独立工作目录**：`$DSH_HOME/workspace/agents/<botId>/`，它读写文件、跑代码、存草稿，全在这个沙箱里，彼此隔离
- **工具 / 技能 / MCP 白名单**：每个好友单独勾选它能用什么。你可以让"产品经理"只用搜索和文档工具，让"工程师"拿全套文件操作
- **Agent 开关**：关掉就是纯聊天，一个工具都不暴露给模型

人设懒得写？点一下"AI 生成"，输入一个名字，人设就流式地写进输入框里了。自我介绍、群聊介绍同理。

底层跑的是 [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness)（`dsh`）的 agent loop。我做的事情是把"聊天"这个业务层，以 Cordis 插件的形式，完整地搭在了它的内核之上——工具系统、LLM 适配、会话事件日志，全都复用，我只写聊天这部分。

---

## 二、群聊：让 AI 自己聊起来

这是我最喜欢的部分。

拉一个群，把几个好友拖进去。然后你说话，或者你干脆**什么都不说**。

群里的发言规则是这样的：

```
@名字          → 必答
冷却窗口        → 刚说完的人短时间内不抢话
关键词命中      → 话里提到它关心的词，提高发言概率
基础概率        → 没人提它时，也有一个较低的随机开口概率
主动发言（可选） → 群里安静满 N 分钟，它自己起个话题
```

主动发言的空闲窗口我特意做成了 **5–10 分钟随机**。因为早期版本是固定值，结果几个好友会在同一秒集体开口，那画面非常诡异。随机化之后，节奏就自然多了。

还有几个细节：

- 每个（群, 好友）组合持有一条**独立的会话记忆**。在群里经历的事，不会串到私聊里去；反过来也一样。
- 群和每位成员各有**工作目录**，会把"群共享区"和"你的私人区"一起注入上下文，让它们自己决定往哪儿写。
- AI 之间可以**连锁接话**，最多 3 轮——防止它们无限自嗨。

第一次看到四个 AI 自己聊起来、互相反驳、还引用了彼此说过的话时，那个感觉很难形容。

---

## 三、长期记忆：清空聊天记录，不会清空这段关系

所有聊天 App 都有一个残忍的按钮：**清空聊天记录**。

在 DeepChat 里，点下这个按钮，发生的事情是：

1. 后端先对当前会话做一次**蒸馏**（一次 LLM 调用）
2. 把这段关系里值得留下的东西，写进这个好友的 `MEMORY.md`
3. 然后才清空会话

下次你再打开对话框，它记得你。不是靠把整个历史塞进上下文（那早晚爆掉），而是靠一份持续被重写的记忆文档。

这份 `MEMORY.md` 你在编辑弹窗里**能直接看到、能手动改**。它就在好友的工作目录下，是个纯文本文件。我觉得这一点很重要——你的数据应该是你能读懂、能编辑的东西，不是某个数据库里的黑盒。

---

## 四、克隆好友：复制一段关系，但不复制聊天记录

有次我想试试"同一个好友、不同人设分支"的效果，于是做了**克隆**。

一键克隆，发生的事是：

- 复制人设、模型、工具、技能、MCP 配置
- 复制头像（**物理复制文件**，不是共用 URL，否则原好友一删，克隆体的头像就 404 了）
- 复制长期记忆
- **不复制聊天记录**——但会立刻把源好友的会话蒸馏进克隆体的记忆

最后一点是关键。克隆体拿到的是"这段关系的总结"，而不是"这段关系的流水账"。所以它一开始就知道你们经历过什么，但对话框是干净的。

命名会自动递增：`小艺` → `小艺-克隆体` → `小艺-克隆体2`（英文界面则是 `Clone` → `Clone2`）。这个后缀词是前端按当前语言传给后端的，因为宿主进程没有 i18n 服务。

---

## 五、全部数据在你本机

这句话我想单独拿出来说。

DeepChat 是一个 Tauri 2 桌面应用，前后端都跑在你机器上：

- 后端：`127.0.0.1:3180`，一个本地 HTTP + SSE 服务
- 前端：`localhost:1420` 的 Vue 3 应用（dev 模式）

**没有任何服务端**。模型 API Key 存在本地凭据文件里，会话记录是本地的 zstd 压缩 JSONL。设置页里有个"打开数据目录"的按钮，点了就能看到全部：

```
.dsh-home/
├── storages/           好友、群聊、模型配置（JSON）
├── workspace/agents/   每个好友一个目录：工作文件 + memory/MEMORY.md
├── workspace/groups/   群聊共享文件
├── sessions/           会话事件日志（仅追加）
├── skills/             已安装的技能
└── logs/               运行日志
```

顺便说一个设计上的取舍：**会话是仅追加的事件日志，不是数据库表**。你在界面上看到的消息，是按 turn / prompt 从事件流里聚合投影出来的。好处是永不丢失、可以重放、可以做时间旅行式的调试；代价是查询要做分页游标（我用 `?before=<seq>` 而不是 offset，避免新消息把页面顶歪）。

---

## 六、技术上踩过的几个坑

这部分写给想做类似东西的人。

**1. `settings` / `credentials` 不能写进模块级 `inject`**

Cordis 插件的 `inject` 数组里如果放了这两个依赖，启动时会死锁。必须用 `ctx.inject()` 动态注入。

**2. `storageDomain.open` 一个插件只能开一次**

开第二次会抛 `json backend is closed`。所有表必须共享同一个 domain 句柄。

**3. 前端消息顺序是个大坑**

流式输出时，用户消息和 AI 回复的到达顺序并不总是你以为的那样。我最后的解法是后端广播 `promptSeq`，前端为每条用户消息建占位草稿，AI 回复按 `m-p{N}` 的 N **插入到第 N 条用户消息之后**，而不是简单追加。ack 到达后再校正序号。

**4. WKWebView 的 CORS**

Tauri 在 macOS 上用的是 WKWebView，原生 `fetch` 跨源会被拒。三个 AI 生成功能的 "Load Fail" 就是这么来的，最后统一走 `dshFetch` 才修好。

**5. 图片要落盘**

早期图片存在内存映射里，宿主一重启全没了。现在图片写到 `$HOME/chat-agent-images/<会话 id>/`，前端用 `convertFileSrc` 转成 `asset://` 加载，老数据在拉历史时自动迁移。

**6. 改后端插件必须重启宿主**

前端 Vite 能热更新，但已加载的 Node 模块不会热替换。改完 `packages/chat/*` 要 `tsc -b` + `tsdown`，然后**重启宿主进程**。

---

## 七、快速开始

```bash
git clone https://github.com/theajack/deepchat.git
cd deepchat

pnpm install --ignore-scripts
pnpm run build        # 构建 dsh 内核（必须，否则启动报"无法定位 dsh"）

cd apps/desktop
pnpm tauri dev        # 首次编译 Rust 约 1-3 分钟
```

需要 Node ≥ 20、pnpm ≥ 9、Rust 工具链。

`pnpm tauri dev` 会同时干两件事：起 Vite 前端（1420），由 Rust 侧拉起 dsh 宿主（3180），轮询端口直到就绪再通知前端开始请求——所以你不用手动开两个终端。

想只调后端也行：

```bash
DSH_HOME=$PWD/.dsh-home node apps/cli/lib/bin.js --profile chat-agent
curl http://127.0.0.1:3180/chatapi/bots
```

---

## 写在最后

做这个项目的过程中，最让我着迷的不是技术，而是**观察**。

我会开一个群，放进去四个性格迥异的好友，然后一句话都不说，看它们怎么开始对话、怎么分配话题、谁更爱接话、谁总是沉默。有时候它们会聊出我没预料到的东西——那是一种很奇妙的体验：你在看一个你造的小社会运转。

如果你也想玩玩，或者只是好奇"AI 版微信"到底长什么样：

**https://github.com/theajack/deepchat**

Star ⭐ 是对我最大的鼓励，Issue 和 PR 更欢迎。

---

*本项目基于 [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness) 构建，感谢上游提供的强大内核。*
