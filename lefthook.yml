# Git hooks (lefthook).
#
# 本地检查钩子已全部移除：pre-commit 的 lint / 翻译配对 / vendor 校验与
# pre-push 的 typecheck 都不再在本地执行，避免阻塞提交与推送。
# 这些检查由 CI 负责（CI 拥有完整的仓库级 gate 矩阵）。
#
# 恢复方式：从 git 历史取回本文件此前的版本。
# Install: `node scripts/install-lefthook.mjs` (runs automatically via postinstall).
